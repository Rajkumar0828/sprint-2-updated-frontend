export const setSearchQuery = (searchQuery) => ({    
    type: 'SET_SEARCH_QUERY',
    payload: searchQuery,
  });


export const setCourse =  (course)=>({
    type: 'SET_COURSE',
    payload: course,
})